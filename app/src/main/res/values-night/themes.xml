<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Notes" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Dark_primaryColor</item>
        <item name="colorPrimaryVariant">@color/Dark_primaryLightColor</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/Dark_secondaryColor</item>
        <item name="colorSecondaryVariant">@color/Dark_secondaryLightColor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="materialCardViewStyle">@style/cardMaterialStyle</item>
    </style>

    <style name="cardMaterialStyle" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardBackgroundColor">@color/Dark_primaryLightColor</item>
        <item name="cardElevation">2dp</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>